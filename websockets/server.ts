// import { io } from "socket.io-client";

// // Conectando ao servidor Socket.IO
// const socket = io("http://localhost:8080");

// // Evento quando a conexão é aberta
// socket.on("connect", () => {
//   console.log("Conectado ao servidor Socket.IO!");
//   socket.emit("message", "Olá, servidor!"); // Envia mensagem ao servidor
// });

// // Evento para mensagens recebidas
// socket.on("message", (data) => {
//   console.log("Mensagem do servidor:", data);
// });

// // Evento quando a conexão é fechada
// socket.on("disconnect", () => {
//   console.log("Conexão com o servidor encerrada");
// });

// // Evento para erros
// socket.on("connect_error", (error) => {
//   console.error("Erro na conexão Socket.IO:", error);
// });
